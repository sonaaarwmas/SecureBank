import AuthCheck from "../../../components/authcheck";
import CreateTransactions from "../../../components/create-transactions";

const CreateTransactionPage: React.FC = () => {
  return (
      <CreateTransactions />
  );
};

export default CreateTransactionPage;
